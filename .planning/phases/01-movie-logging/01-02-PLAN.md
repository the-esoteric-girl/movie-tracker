---
phase: 01-movie-logging
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/pages/MovieDetailPage.jsx
autonomous: false
requirements:
  - LOG-01
  - LOG-05

must_haves:
  truths:
    - "LOG IT button appears on the detail page when the movie is not in seenList"
    - "Clicking LOG IT opens LogModal in add mode with today's date and blank fields"
    - "Saving the modal adds the enriched movie object to seenList"
    - "✓ LOGGED button appears when the movie is already in seenList"
    - "Clicking ✓ LOGGED opens LogModal in edit mode pre-populated with existing rating/review/date"
    - "Saving in edit mode overwrites the seenList entry via updateSeen"
    - "Closing the modal (CANCEL or Escape) dismisses it without modifying seenList"
  artifacts:
    - path: "src/pages/MovieDetailPage.jsx"
      provides: "Modal trigger logic replacing toggleSeen"
      contains: "LogModal"
  key_links:
    - from: "src/pages/MovieDetailPage.jsx"
      to: "LogModal"
      via: "import and conditional render"
      pattern: "import LogModal"
    - from: "src/pages/MovieDetailPage.jsx"
      to: "onAddSeen / onUpdateSeen"
      via: "onSave callback passed to LogModal"
      pattern: "onSave.*onAddSeen|onUpdateSeen"
---

<objective>
Wire LogModal into MovieDetailPage so users can log and edit movies directly from the detail screen.

Purpose: Connects the LogModal built in Plan 01 to the existing detail page, replacing the old toggleSeen toggle with a proper modal flow.
Output: MovieDetailPage.jsx updated with modal state, LOG IT / ✓ LOGGED button states, add flow, and edit flow all working.

Note: LOG-06 ("modal can be triggered from multiple places") is partially satisfied here — the detail page trigger is complete. The SeenPage trigger is Phase 2 work (wired in 02-02). LOG-06 is tracked in Phase 2's requirements until fully done.
</objective>

<execution_context>
@C:/Users/sophi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sophi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sophi/Projects/movie-tracker/.planning/ROADMAP.md
@C:/Users/sophi/Projects/movie-tracker/.planning/phases/01-movie-logging/01-CONTEXT.md
@C:/Users/sophi/Projects/movie-tracker/src/pages/MovieDetailPage.jsx
@C:/Users/sophi/Projects/movie-tracker/src/App.jsx
@C:/Users/sophi/Projects/movie-tracker/.planning/phases/01-movie-logging/01-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 output and existing code. No exploration needed. -->

LogModal props (created in Plan 01):
  import LogModal from "../components/LogModal";

  Props:
    movie        — TMDB movie object (pass the current movie state)
    initialData  — null for add mode, { rating, review, watchedDate } for edit mode
    onSave(data) — called with { rating, review, watchedDate } when user clicks LOG IT
    onClose()    — called when modal should dismiss (cancel, Escape, backdrop)

MovieDetailPage current props (from App.jsx Route):
  watchlist, seenList, onAdd, onRemove, onAddSeen, onRemoveSeen
  + onUpdateSeen (added by Plan 01 to App.jsx)

Current isInSeen logic (line 47 of MovieDetailPage.jsx):
  const isInSeen = movie ? seenList.some((m) => m.id === movie.id) : false;

Current toggleSeen (lines 54-57) — to be REPLACED:
  const toggleSeen = () => {
    if (!movie) return;
    isInSeen ? onRemoveSeen(movie.id) : onAddSeen(movie);
  };

Current watched button JSX (around line 165):
  Classes: styles.btn + styles.btnWatched + (isInSeen ? styles.btnWatchedActive : "")
  onClick={toggleSeen}

Enriched seenList entry shape (from Plan 01):
  { ...tmdbMovieFields, rating: Number, review: String, watchedDate: "YYYY-MM-DD" }

App.jsx updateSeen signature (from Plan 01):
  updateSeen(movieId, fields)
  — merges fields into matched entry and saves to localStorage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire LogModal into MovieDetailPage with add and edit flows</name>
  <files>src/pages/MovieDetailPage.jsx</files>
  <action>
Update src/pages/MovieDetailPage.jsx with these targeted changes only:

1. ADD IMPORT at the top:
   import LogModal from "../components/LogModal";

2. ADD onUpdateSeen to the destructured props:
   export default function MovieDetailPage({ watchlist, seenList, onAdd, onRemove, onAddSeen, onRemoveSeen, onUpdateSeen })

3. ADD modal state alongside existing useState declarations:
   const [showLogModal, setShowLogModal] = useState(false);

4. REPLACE toggleSeen with openLogModal:
   const openLogModal = () => {
     if (!movie) return;
     setShowLogModal(true);
   };

5. UPDATE the watched button in the JSX actions div:
   - Change onClick from toggleSeen to openLogModal
   - Change text: isInSeen ? "✓ LOGGED" : "LOG IT"
   - Keep existing className logic (styles.btn + styles.btnWatched + active class when isInSeen)

6. ADD LogModal conditional render just before the closing div of the outermost .page div:

   {showLogModal && movie && (
     <LogModal
       movie={movie}
       initialData={isInSeen ? (seenList.find((m) => m.id === movie.id) ?? null) : null}
       onSave={(data) => {
         if (isInSeen) {
           onUpdateSeen(movie.id, data);
         } else {
           onAddSeen({ ...movie, ...data });
         }
         setShowLogModal(false);
       }}
       onClose={() => setShowLogModal(false)}
     />
   )}

   initialData:
     - isInSeen true: pass the matching seenList entry (contains rating/review/watchedDate) — edit mode
     - isInSeen false: pass null — add mode (modal defaults to 0 rating, empty review, today)

Do not remove btnWatched/btnWatchedActive CSS classes. Do not change watchlist button, fetch useEffect, or cast section.
  </action>
  <verify>
    <automated>cd /c/Users/sophi/Projects/movie-tracker && node -e "const fs=require('fs'); const page=fs.readFileSync('src/pages/MovieDetailPage.jsx','utf8'); const checks=[['Imports LogModal',page.includes('import LogModal')],['Props include onUpdateSeen',page.includes('onUpdateSeen')],['Has showLogModal state',page.includes('showLogModal')],['Has openLogModal',page.includes('openLogModal')],['toggleSeen removed',!page.includes('toggleSeen')],['LOG IT text present',page.includes('LOG IT')],['Checkmark LOGGED text present',page.includes('\u2713 LOGGED')],['LogModal rendered conditionally',page.includes('showLogModal && movie')],['onSave calls onUpdateSeen',page.includes('onUpdateSeen(movie.id')],['onSave calls onAddSeen',page.includes('onAddSeen')],['onClose sets modal false',page.includes('setShowLogModal(false)')]]; checks.forEach(([name,pass])=>console.log((pass?'PASS':'FAIL')+': '+name));"</automated>
  </verify>
  <done>All checks pass. MovieDetailPage imports LogModal, has showLogModal state, openLogModal replaces toggleSeen, button shows "LOG IT" or "✓ LOGGED", LogModal renders conditionally with correct initialData and onSave/onClose handlers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full Phase 1 logging flow end-to-end</name>
  <files>none</files>
  <action>
Human verifies the complete logging flow works correctly in the running app. No code changes — this task confirms the automation above works as intended.
  </action>
  <verify>User confirms all steps below pass during manual testing.</verify>
  <done>User types "approved" after testing all 15 verification steps.</done>
  <what-built>
    Full Phase 1 logging flow:
    - LogModal component (half-star rating, date picker, review textarea, dirty-aware cancel)
    - App.jsx extended with updateSeen
    - MovieDetailPage wired with LOG IT / ✓ LOGGED button states and modal open/close
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Navigate to any movie detail page (click a movie from Find)
    3. Verify the button reads "LOG IT" (not "MARK WATCHED")
    4. Click "LOG IT" — modal should open with:
       - Pink "I WATCHED..." title
       - Small movie poster + title + year
       - 5 lime-colored star positions (empty by default)
       - "DATE" label + today's formatted date (e.g. "Sunday, March 9, 2026")
       - Empty "My honest thoughts..." textarea
       - "CANCEL" (plain text, left) + "LOG IT" (lavender filled button, right)
    5. Hover over the left half of star 3 — numeric display should show "2.5/5"
    6. Hover over the right half of star 3 — numeric display should show "3.0/5"
    7. Click the right half of star 4 — rating locks at "4.0/5"
    8. Type something in the review textarea — "CANCEL" should change to "DISCARD CHANGES" in pink
    9. Click the date text — native date picker should appear; change the date and verify it updates
    10. Click LOG IT — modal closes; button on detail page now reads "✓ LOGGED" (with checkmark)
    11. Confirm "✓ LOGGED" button shows the active styling (lavender or distinct color) and the checkmark character is visible
    12. Click "✓ LOGGED" — modal reopens pre-populated with the rating, review, and date you entered
    13. Change the rating and click LOG IT — verify the entry updates (re-open modal to confirm new values)
    14. Open modal and press Escape — modal should close without saving
    15. Open modal, make a change, click the dark backdrop — modal should close without saving
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After Task 1, verify the build compiles:
  cd /c/Users/sophi/Projects/movie-tracker && npm run build

Expected: Zero build errors. Requirements LOG-01 and LOG-05 (from detail page) are implemented. LOG-06 is partially fulfilled here (detail page trigger) — SeenPage trigger completes LOG-06 in Phase 2.
</verification>

<success_criteria>
- MovieDetailPage shows "LOG IT" for unlogged movies and "✓ LOGGED" for logged movies
- Clicking either opens LogModal in the correct mode (add or edit)
- Saving a new log calls onAddSeen with enriched { ...movie, rating, review, watchedDate }
- Saving an edit calls onUpdateSeen(movie.id, { rating, review, watchedDate })
- Closing modal dismisses without side effects
- npm run build succeeds
- Human verification checkpoint passes (user types "approved")
</success_criteria>

<output>
After completion, create .planning/phases/01-movie-logging/01-02-SUMMARY.md
</output>
