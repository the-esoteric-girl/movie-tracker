---
phase: 02-seen-page
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/pages/SeenPage.jsx
  - src/App.jsx
autonomous: false
requirements:
  - SEEN-03
  - LOG-06

must_haves:
  truths:
    - "Tapping any movie card in grid view opens the LogModal pre-populated with that movie's rating, review, and watched date"
    - "Tapping any movie row in list view opens the LogModal pre-populated with that movie's data"
    - "Saving in the modal updates the entry in seenList — does not add a duplicate"
    - "LogModal can be triggered from both SeenPage and MovieDetailPage (LOG-06 complete)"
    - "Closing the modal returns to the Seen page without any state change"
  artifacts:
    - path: "src/pages/SeenPage.jsx"
      provides: "LogModal wired with selectedEntry state + onSave/onClose handlers"
      contains: "showLogModal"
    - path: "src/App.jsx"
      provides: "onUpdateSeen prop passed to SeenPage route"
      contains: "onUpdateSeen={updateSeen}"
  key_links:
    - from: "src/pages/SeenPage.jsx"
      to: "src/components/LogModal.jsx"
      via: "import LogModal + conditional render on showLogModal"
      pattern: "showLogModal && selectedEntry"
    - from: "src/pages/SeenPage.jsx"
      to: "onUpdateSeen prop"
      via: "called inside LogModal's onSave handler"
      pattern: "onUpdateSeen"
    - from: "src/App.jsx"
      to: "SeenPage route"
      via: "onUpdateSeen={updateSeen} prop"
      pattern: "onUpdateSeen"
---

<objective>
Wire LogModal into SeenPage so tapping any grid card or list row opens the modal pre-populated with that entry's existing log data. Saving updates the entry via onUpdateSeen. Also adds the missing onUpdateSeen prop to SeenPage's route in App.jsx, completing LOG-06.

Purpose: Closes the loop on the seen page — users can browse their log and edit any entry without navigating away.
Output: SeenPage.jsx (modal state + handlers added), App.jsx (one prop addition)
</objective>

<execution_context>
@C:/Users/sophi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sophi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seen-page/02-CONTEXT.md
@.planning/phases/02-seen-page/02-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/components/LogModal.jsx (already built in Phase 1):
  export default function LogModal({ movie, initialData, onSave, onClose })
  movie: full TMDB movie object (seenList entry works — it contains all TMDB fields)
  initialData: { rating, review, watchedDate } — pre-populates the form
  onSave: called with { rating, review, watchedDate } when user clicks LOG IT
  onClose: called when user cancels or clicks backdrop

From src/App.jsx — updateSeen function:
  function updateSeen(movieId, fields) {
    const updated = seenList.map((m) =>
      m.id === movieId ? { ...m, ...fields } : m,
    );
    setSeenList(updated);
    saveToStorage("seenList", updated);
  }

SeenPage route (CURRENT — needs onUpdateSeen added):
  element={<SeenPage seenList={seenList} onRemove={removeFromSeen} />}

Change to:
  element={
    <SeenPage
      seenList={seenList}
      onRemove={removeFromSeen}
      onUpdateSeen={updateSeen}
    />
  }

From src/pages/SeenPage.jsx (produced by plan 02-01):
- Grid cards have onClick={() => {}} — replace with () => openModal(entry)
- List rows have onClick={() => {}} — replace with () => openModal(entry)
- Props already include onUpdateSeen in destructuring (added defensively in plan 01)

seenList entry shape (reminder):
  {
    id, title, poster_path, release_date, vote_average,  // TMDB fields
    rating, review, watchedDate                          // Phase 1 extended fields
  }
  The entry object itself can be passed as movie to LogModal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LogModal state and handlers to SeenPage</name>
  <files>src/pages/SeenPage.jsx</files>
  <action>
Add modal wiring to the SeenPage component produced by plan 02-01.

1. Add import at top:
   import LogModal from "../components/LogModal";

2. Add two state variables inside SeenPage component:
   const [showLogModal, setShowLogModal] = useState(false);
   const [selectedEntry, setSelectedEntry] = useState(null);

3. Add openModal helper:
   function openModal(entry) {
     setSelectedEntry(entry);
     setShowLogModal(true);
   }

4. Replace the no-op onClick on grid cards:
   Change onClick={() => {}} to onClick={() => openModal(entry)}
   (applies to the gridCard div)

5. Replace the no-op onClick on list rows:
   Change onClick={() => {}} to onClick={() => openModal(entry)}
   (applies to the listRow div)

6. Add LogModal render at the bottom of the return, inside the outermost wrapper div, after the grid/list content:

   {showLogModal && selectedEntry && (
     <LogModal
       movie={selectedEntry}
       initialData={{
         rating: selectedEntry.rating,
         review: selectedEntry.review,
         watchedDate: selectedEntry.watchedDate,
       }}
       onSave={(data) => {
         onUpdateSeen(selectedEntry.id, data);
         setShowLogModal(false);
         setSelectedEntry(null);
       }}
       onClose={() => {
         setShowLogModal(false);
         setSelectedEntry(null);
       }}
     />
   )}

The onSave handler calls onUpdateSeen (updating the entry in-place) then closes. This never calls onRemove + re-add — it is always an update because the entry already exists in seenList.
  </action>
  <verify>
    <automated>cd C:/Users/sophi/Projects/movie-tracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SeenPage imports LogModal
    - showLogModal and selectedEntry state added
    - Grid card and list row onClick handlers call openModal(entry)
    - LogModal renders conditionally with selectedEntry as movie and initialData
    - onSave calls onUpdateSeen(selectedEntry.id, data) then clears modal state
    - Build passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass onUpdateSeen to SeenPage route in App.jsx</name>
  <files>src/App.jsx</files>
  <action>
In App.jsx, find the SeenPage route:
  element={<SeenPage seenList={seenList} onRemove={removeFromSeen} />}

Add onUpdateSeen={updateSeen} prop:
  element={
    <SeenPage
      seenList={seenList}
      onRemove={removeFromSeen}
      onUpdateSeen={updateSeen}
    />
  }

No other changes to App.jsx. The updateSeen function already exists from Phase 1.
  </action>
  <verify>
    <automated>cd C:/Users/sophi/Projects/movie-tracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SeenPage route in App.jsx includes onUpdateSeen={updateSeen}
    - Build passes with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify SeenPage and modal flow in browser</name>
  <files>src/pages/SeenPage.jsx, src/App.jsx</files>
  <action>
Human verification of the complete SeenPage experience. All automated tasks have run — this checkpoint confirms the full UI flow works correctly in the browser.

Steps:
1. Run: npm run dev
2. Log at least one movie via Find tab (open any movie, click LOG IT, set a rating and review, save)
3. Navigate to Seen tab — confirm movie appears in grid with personal star rating (not TMDB rating)
4. Click the GRID / LIST toggle — confirm list view shows title + rating + review snippet + watched date
5. Tap a movie in grid view — confirm LogModal opens pre-populated with saved rating/review/date
6. Change the rating in the modal, click LOG IT — confirm the grid card updates to show the new rating
7. Tap a movie in list view — confirm LogModal also opens from list view
8. Clear localStorage key seenList in DevTools, refresh — confirm inviting empty state with Find tab link appears
  </action>
  <verify>
    <automated>MISSING — visual/interactive checkpoint requiring human verification in browser</automated>
  </verify>
  <done>User confirms: grid shows personal ratings, list view shows diary rows, tapping opens pre-populated LogModal, saving updates the entry, empty state is inviting with a link to the Find tab</done>
</task>

</tasks>

<verification>
1. npm run build exits with code 0
2. App.jsx SeenPage route includes onUpdateSeen={updateSeen}
3. SeenPage.jsx imports LogModal
4. Tapping a card opens LogModal with pre-populated data (visual check in checkpoint)
5. Saving in modal calls onUpdateSeen — no duplicate entries created
6. LOG-06 satisfied: LogModal triggerable from MovieDetailPage (Phase 1) and SeenPage (this plan)
</verification>

<success_criteria>
- Grid and list view cards/rows open LogModal on tap
- LogModal pre-populates with existing rating, review, watchedDate
- Saving updates the entry (not re-adding)
- App.jsx passes onUpdateSeen to SeenPage
- Human verification confirms full end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/02-seen-page/02-02-SUMMARY.md` using the summary template.
</output>
